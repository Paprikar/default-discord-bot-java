# logging
logging.level:
  root: warn
  dev.paprikar.defaultdiscordbot: info
  net.dv8tion.jda: info
  com.vk.api.sdk: info

# persistence
spring:
  jpa:
    generate-ddl: true
    hibernate.ddl-auto: validate
    open-in-view: false
